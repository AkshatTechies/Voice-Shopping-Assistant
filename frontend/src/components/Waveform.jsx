/**
 * Waveform — placeholder for Phase 2.
 * Driven by real Web Audio AnalyserNode data starting Phase 3.
 * Not rendered anywhere yet; exists so the file/component contract
 * from Section 7 of the spec is in place.
 */

export default function Waveform() {
  return (
    <div className="flex h-12 items-center justify-center gap-1 text-slate-300">
      <span className="text-xs">[ waveform placeholder — Phase 3 ]</span>
    </div>
  )
}
