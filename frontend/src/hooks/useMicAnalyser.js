/**
 * useMicAnalyser — stub for Phase 2.
 * Real Web Audio AnalyserNode wiring (for the live waveform) arrives
 * alongside useAudioRecorder in Phase 3, feeding the Phase 7 animation.
 */

export function useMicAnalyser() {
  return { levels: [] }
}
